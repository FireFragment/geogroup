[package]
name = "geogroup_algo"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
geo = { version = "0.28.0", optional = true }
geogroup_common = { path = "../common" }
egui_struct = {version = "0.4", optional = true}

[features]
geo = ["dep:geo"]
default = ["geo"]
